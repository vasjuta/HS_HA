1. Install (python3.7+)
   pip3 install sanic==21.3.4

2. Run API
   python3 main.py

3. Use API
   curl -XPOST localhost:4040/hello

   curl -XPOST localhost:4040/predict_genres -d '{"plot": "Four friends enter the woods at night where they are killed by an evil sociopath"}'

